# JSON Resume Manager

```bash
# Vue auth
composer require laravel/ui
php artisan ui vue --auth
npm install
npm run watch

# Images
composer require intervention/image

# API Tokens
composer require laravel/sanctum

# Vue Tests
npm i --save-dev jest @vue/test-utils babel-jest babel-core@^7.0.0-0 @babel/preset-env vue-jest
```
